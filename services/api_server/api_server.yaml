apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: api-server
spec:
  template:
    spec:
      containers:
        - image: raptorchoinl/serverless-k8s:latest
          ports:
            - containerPort: 8080